---
argument_specs:
  main:
    short_description: Main entrypoint for connect_vpn_client role
    options:
      vpn_client_config_local_path:
        type: path
        required: true
        description: Path to OpenVPN client config on local machine
